/* 
    *   Child Nodes in JS

    1. Text Node
    2. Element Node
    3. Comment Node
    4. Attribute Node
*/

console.log(document.documentElement);  // returns html as an object
console.log(document.body.childNodes);  // returns all the child nodes of body as an collection called Nodelist
// by default all the spacing between elements is considered as text node